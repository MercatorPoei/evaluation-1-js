// declaration constant name
const name = 'Riri';
// affichage de name
console.log(name);

// declaration time
let time = 10;
// ajout +2 à time
time = time + 2;
// affichage de time dans console
console.log(time);

// declaration du boolean
let done = false;
// attribution de true a variable done
done = true;
// affichage de done dans console
console.log(done);

